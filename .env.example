# Key for data.gov.in, used only by the offline pipeline in pipeline/.
# Register at https://data.gov.in to get one. Nothing in the site needs it at
# request time; the pipeline writes data/base_rate.json and that file is committed.
DATA_GOV_KEY=
